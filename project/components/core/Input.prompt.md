Labelled text input with focus ring, optional leading icon, hint and error text.

```jsx
<Input label="Nama Lengkap" placeholder="Sesuai paspor" />
<Input label="No. WhatsApp" iconLeft={<Phone size={16} />} hint="Aktif untuk konfirmasi" />
<Input label="Email" error="Format email belum benar" />
```
